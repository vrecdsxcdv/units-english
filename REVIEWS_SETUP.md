# 🔧 Настройка системы отзывов

## 🚀 Архитектура

Система состоит из двух серверов:
- **Next.js** (Frontend): `http://localhost:3000`
- **Reviews API** (Backend): `http://localhost:3001`

## ✅ Сервера запущены!

- Reviews API работает на порту **3001**
- Next.js работает на порту **3000**

## 🔑 Токен администратора

Для доступа к админ-панели используйте этот токен:

```
6d782f0d08188193887b5b9f00920040718f64de3ffa8f0dbbb0d4fdbe5e0d7a
```

**Где использовать:**
1. Откройте админ-панель: http://localhost:3000/admin/reviews
2. Вставьте токен в поле ввода
3. Нажмите "Войти"

## 📝 Как работает система

### Для пользователей:
1. Открыть страницу отзывов: http://localhost:3000/reviews
2. Нажать кнопку отправки отзыва
3. Заполнить имя (2-40 символов) и текст (10-500 символов)
4. Отзыв отправится на модерацию

**Ограничения:** 5 отзывов за 15 минут с одного IP

### Для администратора:
1. Войти в админ-панель с токеном (см. выше)
2. Просмотреть все отзывы (одобренные + на модерации)
3. Одобрить/отклонить/удалить отзывы
4. Использовать фильтры и поиск

## 🚀 Быстрый старт

### Если сервер не запущен:

```bash
cd server
npm run dev
```

### Проверка работы сервера:

```bash
curl http://localhost:3001/health
# Должно вернуть: {"status":"ok","timestamp":"..."}
```

### Тестовая отправка отзыва:

```bash
curl -X POST http://localhost:3001/reviews \
  -H "Content-Type: application/json" \
  -d '{"id":"Иван","text":"Отличный курс английского языка!"}'
```

## 🔒 Безопасность

✅ Санитизация ввода (защита от XSS)
✅ Rate limiting (5 отзывов/15 мин)
✅ Токен администратора обязателен
✅ CORS настроен для localhost

## 📁 Важные файлы

- `server/.env` - конфигурация сервера (содержит ADMIN_TOKEN)
- `server/reviews.db` - база данных SQLite с отзывами
- `server/README.md` - подробная документация API

## 🔧 Если что-то не работает

### Сервер не запускается:
```bash
cd server
cat .env  # Проверьте, что ADMIN_TOKEN установлен
npm run dev  # Запустите сервер
```

### CORS ошибки в браузере:
Убедитесь, что:
1. Сервер запущен на порту 3001
2. Frontend запущен на порту 3000
3. В `server/.env` переменная `ALLOWED_ORIGINS` пустая

### Не могу войти в админ-панель:
Используйте токен из этого файла (см. раздел "🔑 Токен администратора")

## 📊 Проверить статус

```bash
# Проверить, запущен ли сервер
lsof -i:3001

# Просмотреть логи сервера (если запущен в фоне)
# (ID процесса можно найти через lsof)

# Проверить количество отзывов в БД
cd server
sqlite3 reviews.db "SELECT COUNT(*) FROM reviews;"
```
