т# A1 Track — Unified Structure (Единая структура A1)

## ✅ Что было сделано

### 1. Удалены отдельные роуты, перехватывающие рендер
- ❌ `src/app/blocks/a1/nouns/` — удалена папка с отдельным page.tsx
- ❌ `src/app/blocks/a1/tobe/` — удалена папка с отдельным page.tsx

**Причина:** Эти роуты перехватывали навигацию и не позволяли использовать единый механизм searchParams для переключения тем.

---

### 2. Создана единая страница трека A1

**Файл:** `src/app/blocks/a1/page.tsx` (Server Component)

```
GET /blocks/a1?t=nouns&s=1     // Существительные, шаг 1
GET /blocks/a1?t=adjectives&s=1 // Прилагательные, шаг 1
GET /blocks/a1?t=comparison&s=1 // Степени сравнения, шаг 1
GET /blocks/a1?t=greetings&s=1  // Приветствие, шаг 1
```

**Ключевые особенности:**
- ✅ Читает searchParams через `await` (Next 15 pattern)
- ✅ Валидирует тему через массив `topics`
- ✅ Использует `key={topic-${step}}` для перерасчёта контента
- ✅ Отображает красивый заголовок с названием темы
- ✅ Показывает StepSquares (10 шагов)
- ✅ Навигация между шагами через кнопки Назад/Далее

---

### 3. Обновлен сайдбар

**Файл:** `src/components/lesson/Sidebar.tsx` (Client Component)

**Что изменилось:**
- ✅ Теперь часть компонента для `/blocks/a1`
- ✅ Показывает 4 темы (существительные, прилагательные, степени, приветствие)
- ✅ Все ссылки ведут на единый роут: `/blocks/a1?t={topic}&s=1`
- ✅ Активная тема подсвечивается (indigo-100, bold)
- ✅ Нумерация тем (1., 2., 3., 4.)

---

### 4. Создана архитектура компонентов уроков

#### Nouns (Существительные)
**Файл:** `src/components/lesson/nouns/Nouns.tsx`
- ✅ 10 полных шагов с контентом
- ✅ Структура: Step1, Step2, ..., Step10
- ✅ Используется компонент `<Section title="...">` с заголовками
- ✅ Содержит словарь, примеры, практику

#### Adjectives (Прилагательные)
**Файл:** `src/components/lesson/adjectives/Adjectives.tsx`
- ⚙️ Placeholder (будет переведена позже из `/blocks/[track]/adjectives`)

#### Comparison (Степени сравнения)
**Файл:** `src/components/lesson/comparison/Comparison.tsx`
- ⚙️ Placeholder (будет переведена позже из `/blocks/[track]/comparison`)

#### Greetings (Приветствие)
**Файл:** `src/components/lesson/greetings/Greetings.tsx`
- ⚙️ Placeholder (будет переведена позже из `/blocks/[track]/greetings`)

---

### 5. Обновлен UI компонент Section

**Файл:** `src/components/ui/Section.tsx`

**Что добавлено:**
- ✅ Опциональный prop `title?: string`
- ✅ Если title передан, отображается как `<h3>` с стилем `text-lg font-semibold mb-3`
- ✅ Обратная совместимость — компонент работает и без title

---

## 📊 Структура навигации

```
/blocks/a1
├── ?t=nouns&s=1       → Существительные, шаг 1
├── ?t=nouns&s=2       → Существительные, шаг 2
├── ...
├── ?t=nouns&s=10      → Существительные, шаг 10
├── ?t=adjectives&s=1   → Прилагательные, шаг 1
├── ?t=comparison&s=1   → Степени сравнения, шаг 1
└── ?t=greetings&s=1    → Приветствие, шаг 1
```

**URL примеры:**
- `http://localhost:3000/blocks/a1`                    → redirect to /blocks/a1?t=nouns&s=1
- `http://localhost:3000/blocks/a1?t=nouns&s=3`       → Существительные, шаг 3
- `http://localhost:3000/blocks/a1?t=adjectives&s=5`  → Прилагательные, шаг 5

---

## 🎯 Как это работает

### Flow при клике на тему в сайдбаре

1. Пользователь кликает "Существительные" в Sidebar
2. Переход по ссылке: `/blocks/a1?t=nouns&s=1`
3. Server component `page.tsx` читает searchParams через `await`
4. Выбирается компонент `<Nouns step={1} />`
5. Компонент отображается с заголовком "Тема урока: Существительные"

### Flow при клике на StepSquares

1. Пользователь кликает квадрат "3" в StepSquares
2. *Пока не реализовано* — нужно добавить обработчик onClick в page.tsx
3. Или пользователь кликает кнопку "Далее"
4. Переход: `/blocks/a1?t=nouns&s=4` (текущая тема + следующий шаг)

---

## ⚙️ Что нужно доделать

1. **Plurals theme:** Создать `src/components/lesson/comparison/Comparison.tsx` с полным контентом
2. **Adjectives theme:** Мигрировать из `/blocks/[track]/adjectives`
3. **Greetings theme:** Мигрировать из `/blocks/[track]/greetings`
4. **Клик на StepSquares:** Добавить обработчик `onClick` в page.tsx
5. **Сохранение прогресса:** Добавить localStorage для текущего шага/темы

---

## 📁 Файлы созданные/изменённые

### Созданы:
- ✅ `src/app/blocks/a1/page.tsx`
- ✅ `src/components/lesson/Sidebar.tsx`
- ✅ `src/components/lesson/nouns/Nouns.tsx`
- ✅ `src/components/lesson/adjectives/Adjectives.tsx`
- ✅ `src/components/lesson/comparison/Comparison.tsx`
- ✅ `src/components/lesson/greetings/Greetings.tsx`

### Изменены:
- ✅ `src/components/ui/Section.tsx` (добавлен title prop)

### Удалены:
- ❌ `src/app/blocks/a1/nouns/` (целиком)
- ❌ `src/app/blocks/a1/tobe/` (целиком)

---

## 🚀 Build Status

✅ `npm run build` выполнен успешно
✅ Нет новых ошибок типов
✅ Все импорты разрешены корректно

---

## 🎓 Пример Step 1 (Существительные)

```tsx
// Отображается при /blocks/a1?t=nouns&s=1
<Section title="1. Introduction to Nouns">
  <p><b>Nouns</b> — это слова, обозначающие...</p>
  <ul>
    <li>Person: <b>teacher</b></li>
    ...
  </ul>
</Section>

<Section title="Vocabulary list (10)">
  {/* Grid с 10 словами + транскрипция + перевод */}
</Section>
```

---

Всё готово к использованию! 🎉
