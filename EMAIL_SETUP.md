# Настройка Email верификации и восстановления пароля

## ✅ Реализованный функционал

### 1. **Email верификация при регистрации**
- При регистрации нового пользователя автоматически отправляется письмо с подтверждением
- Токен действителен 24 часа
- Страница подтверждения: `/verify-email?token=...`

### 2. **Восстановление пароля**
- Пользователь может запросить восстановление через страницу `/forgot-password`
- Токен действителен 1 час
- Страница сброса пароля: `/reset-password?token=...`

## 📝 Настройка Email

### Шаг 1: Создание App Password для Gmail

1. Перейдите на https://myaccount.google.com/security
2. Включите двухфакторную аутентификацию (если еще не включена)
3. Перейдите в "App passwords" (Пароли приложений)
4. Создайте новый пароль для приложения "Mail"
5. Скопируйте сгенерированный пароль

### Шаг 2: Настройка переменных окружения

Добавьте следующие переменные в ваш `.env.local`:

```env
# Email Configuration
EMAIL_HOST="smtp.gmail.com"
EMAIL_PORT="587"
EMAIL_SECURE="false"
EMAIL_USER="your-email@gmail.com"
EMAIL_PASSWORD="ваш-app-password-без-пробелов"

# Убедитесь, что NEXTAUTH_URL настроен
NEXTAUTH_URL="http://localhost:3000"  # или ваш production URL
```

### Шаг 3: Перезапустите сервер

```bash
npm run dev
```

## 🧪 Тестирование

### Тест email верификации:

1. Зарегистрируйте нового пользователя на `/register`
2. Проверьте консоль сервера на наличие ошибок отправки email
3. Проверьте почтовый ящик (включая папку "Спам")
4. Нажмите на ссылку подтверждения в письме
5. Вы должны быть перенаправлены на `/login`

### Тест восстановления пароля:

1. Перейдите на `/login`
2. Нажмите "Забыли пароль?"
3. Введите email зарегистрированного пользователя
4. Проверьте почту
5. Нажмите на ссылку восстановления
6. Введите новый пароль
7. Войдите с новым паролем

## 🗄️ Структура базы данных

Были добавлены следующие таблицы:

### EmailVerificationToken
- `id` - уникальный идентификатор
- `email` - email пользователя
- `token` - токен верификации (32 байта, hex)
- `expires` - срок действия токена
- `createdAt` - дата создания

### PasswordResetToken
- `id` - уникальный идентификатор
- `email` - email пользователя
- `token` - токен сброса (32 байта, hex)
- `expires` - срок действия токена
- `createdAt` - дата создания

## 📁 Созданные файлы

### Backend (API):
- `src/lib/email.ts` - утилиты для отправки email
- `src/app/api/auth/verify-email/route.ts` - API подтверждения email
- `src/app/api/auth/forgot-password/route.ts` - API запроса восстановления
- `src/app/api/auth/reset-password/route.ts` - API сброса пароля
- Обновлен `src/app/api/register/route.ts` - добавлена отправка верификации

### Frontend (Pages):
- `src/app/verify-email/page.tsx` - страница подтверждения email
- `src/app/forgot-password/page.tsx` - страница запроса восстановления
- `src/app/reset-password/page.tsx` - страница сброса пароля
- Обновлен `src/app/login/page.tsx` - добавлена ссылка "Забыли пароль?"

### Database:
- `prisma/schema.prisma` - добавлены модели токенов

## 🔧 Production Deployment

Для production развертывания:

1. Настройте SMTP сервер (можно использовать Gmail, SendGrid, AWS SES и т.д.)
2. Добавьте переменные EMAIL_* в настройки вашего хостинга
3. Убедитесь, что NEXTAUTH_URL указывает на production URL
4. Запустите миграцию: `npx prisma db push`

## ⚠️ Важные замечания

1. **Безопасность токенов**: Токены генерируются с использованием crypto.randomBytes(32)
2. **Время жизни**:
   - Email верификация: 24 часа
   - Восстановление пароля: 1 час
3. **Очистка токенов**: Использованные токены автоматически удаляются
4. **Ошибки email**: Если отправка email не удалась при регистрации, регистрация все равно проходит (пользователь может запросить повторную отправку)

## 🐛 Troubleshooting

### Email не отправляются:

1. Проверьте переменные EMAIL_* в .env.local
2. Убедитесь, что используете App Password для Gmail (не основной пароль)
3. Проверьте консоль сервера на наличие ошибок
4. Убедитесь, что nodemailer установлен: `npm install nodemailer`

### Ошибка "Invalid token":

1. Токен мог истечь (проверьте время жизни)
2. Токен уже был использован
3. Проверьте, что URL содержит правильный параметр `?token=...`

### База данных:

Если возникают ошибки с БД:
```bash
npx prisma generate
npx prisma db push
```

## 📚 Дополнительные ресурсы

- [Nodemailer документация](https://nodemailer.com/)
- [Gmail App Passwords](https://support.google.com/accounts/answer/185833)
- [Prisma документация](https://www.prisma.io/docs/)
